{% extends 'base.html.twig' %}

{% block title %}Hello EmployerController!{% endblock %}

{% block body %}
    <h1>Liste des employés</h1>
    {% for employe in employes %}
        <a href="{{ path('show_employe', {'id' :employe.id})}}">{{ employe }}</a> ({{employe.entreprise.raisonSociale}}) <br>
    {% endfor %}
{% endblock %}
